<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Post</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white flex justify-center items-center min-h-screen">
  <form action="/edit/<%= post._id %>" method="POST" class="bg-zinc-800 p-6 rounded-md space-y-4 w-full max-w-md">
    <h2 class="text-xl font-bold">Edit Your Post</h2>
    <textarea name="content" class="w-full h-40 p-3 bg-zinc-700 rounded-md" required><%= post.content %></textarea>
    <button type="submit" class="bg-yellow-500 hover:bg-yellow-600 px-4 py-2 rounded-md font-semibold text-black">Update</button>
  </form>
</body>
</html>
